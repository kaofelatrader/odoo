<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_account_bank_statement_suspense_wizard_form" model="ir.ui.view">
            <field name="name">account.statement.import.statement.wizard.form</field>
            <field name="model">account.statement.suspense.wizard</field>
            <field name="arch" type="xml">
                <form string="Import to suspense account">
                    <group>
                        <p>Select the suspense account where to Import all transactions.</p>
                        <p>You will have to reconcile these entries.</p>
                        <field name="account_id"/>
                    </group>
                    <footer>
                        <button string="Import" name="generate_move_to_suspense" type="object" class='btn-primary'/>
                        <button string="Cancel" class="btn-secondary" special="cancel" />
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_account_bank_statement_to_suspense" model="ir.actions.act_window">
            <field name="name">Import bank statement to suspense account</field>
            <field name="res_model">account.statement.suspense.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>